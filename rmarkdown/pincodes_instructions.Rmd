#### 1. Enter in the table below the pin codes corresponding to the different users.
Right-click to add/delete rows (important: remove rows one at a time due to a minor stability issue currently being fixed).

